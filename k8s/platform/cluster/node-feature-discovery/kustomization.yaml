apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: platform-cluster
resources:
  - repository.yaml
  - release.yaml
  - module-signing/
configMapGenerator:
  - name: values-nfd
    files:
      - values.yaml=values.yaml
