apiVersion: v1
kind: Namespace
metadata:
  name: platform-network
  annotations:
    kubernetes.io/description: >-
      Ingress and egress. Holds the components listed in
      k8s/platform/network/kustomization.yaml.
    kustomize.toolkit.fluxcd.io/prune: disabled
  labels:
    pod-security.kubernetes.io/enforce: baseline
