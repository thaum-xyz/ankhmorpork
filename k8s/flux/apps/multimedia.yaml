apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: multimedia
  namespace: flux-system
spec:
  suspend: true # FIXME(paulfantom): Temporary measure to prevent re-sync of all apps
  interval: 60m0s
  path: ./k8s/apps/multimedia
  prune: true
  sourceRef:
    kind: GitRepository
    name: ankhmorpork
