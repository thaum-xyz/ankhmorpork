apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 15m0s
  path: ./k8s/platform/cluster/flux-system
  prune: false # NOTE: Prevent cascading deletion of the whole cluster
  sourceRef:
    kind: GitRepository
    name: ankhmorpork
