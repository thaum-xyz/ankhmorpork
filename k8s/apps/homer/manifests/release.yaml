apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: homer-operator
  namespace: homer
spec:
  chart:
    spec:
      chart: homer-operator
      version: "1.2.3"
      sourceRef:
        kind: HelmRepository
        name: homer-operator
        namespace: homer
      interval: 15m
  interval: 30m
  timeout: 10m
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  valuesFrom:
    - kind: ConfigMap
      name: homer-values
