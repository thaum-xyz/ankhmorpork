apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homer
resources:
  - repository.yaml
  - release.yaml
  - ingress.yaml
  - ingress-cloudflare.yaml
  - slo-probe-success.yaml
generatorOptions:
  disableNameSuffixHash: true
  labels:
    reconcile.fluxcd.io/watch: Enabled
configMapGenerator:
  - name: values-homer-operator
    files:
      - values.yaml=values.yaml
