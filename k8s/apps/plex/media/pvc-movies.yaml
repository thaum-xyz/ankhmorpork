apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-movies
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Ti
  storageClassName: manual
  volumeName: plex-movies
