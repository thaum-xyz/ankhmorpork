apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: tika
    app.kubernetes.io/name: paperless
  name: tika
  namespace: paperless
spec:
  ports:
    - name: http
      port: 9998
      targetPort: http
  selector:
    app.kubernetes.io/component: tika
    app.kubernetes.io/name: paperless
