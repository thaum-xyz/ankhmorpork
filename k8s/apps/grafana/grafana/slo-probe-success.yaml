apiVersion: pyrra.dev/v1alpha1
kind: ServiceLevelObjective
metadata:
  name: grafana-probe-success
spec:
  # /api/health reports database status, so this fails when Grafana's DB is
  # unreachable and not only when the process dies.
  #
  # target is a PLACEHOLDER (#1379): the probe endpoint changed on 2026-09-06
  # 13:58 UTC, so the first 7d window measuring one endpoint throughout closes
  # 2026-09-13 13:58 UTC. Provisionally 99.975% over the first 3 days.
  description: >-
    Fraction of successful blackbox probes against grafana.krupa.net.pl.
  alerting:
    name: GrafanaProbeBudgetBurn
    absentName: GrafanaProbeMetricAbsent
    severities:
      fastBurn: info
      mediumBurn: info
      slowBurn: none
      longTermBurn: none
      absent: warning
  indicator:
    bool_gauge:
      metric: probe_success{instance=~"https://grafana\\.krupa\\.net\\.pl(/.*)?"}
  target: "95.0"
  window: 7d