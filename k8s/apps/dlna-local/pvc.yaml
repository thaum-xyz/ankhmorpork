apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photos
  namespace: dlna-local
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4000Gi
  storageClassName: manual
  volumeName: photos
