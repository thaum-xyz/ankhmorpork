apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: transmission
  name: transmission
spec:
  ports:
    - port: 9091
      protocol: TCP
      targetPort: http-api
  selector:
    app.kubernetes.io/name: transmission
