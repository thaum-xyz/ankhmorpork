apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sonarr-api-key
spec:
  data:
    - remoteRef:
        key: SONARR_API_KEY
      secretKey: apiKey
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: doppler-auth-api
  target:
    name: sonarr-api-key
