apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cronjob.yaml
  - pvc-config.yaml
configMapGenerator:
  - name: recyclarr-config
    files:
      - recyclarr.yml=config.yaml
generatorOptions:
  disableNameSuffixHash: true
