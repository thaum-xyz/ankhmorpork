apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cleanuparr-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: piraeus-r2-roaming
