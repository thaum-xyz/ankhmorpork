apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: gotenberg
    app.kubernetes.io/name: paperless
  name: gotenberg
  namespace: paperless
spec:
  ports:
    - name: http
      port: 3000
      targetPort: http
  selector:
    app.kubernetes.io/component: gotenberg
    app.kubernetes.io/name: paperless
