apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: pooler
  namespace: paperless
spec:
  selector:
    matchLabels:
      cnpg.io/poolerName: pooler
  podMetricsEndpoints:
  - port: metrics
