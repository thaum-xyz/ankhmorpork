apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: multimedia
  namespace: pics-mgmt
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 4000Gi
  storageClassName: manual
  volumeName: multimedia