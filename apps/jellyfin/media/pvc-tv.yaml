apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-tv
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 6Ti
  storageClassName: manual
  volumeName: media-tv
