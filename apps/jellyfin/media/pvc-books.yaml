apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-books
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: manual
  volumeName: media-books
