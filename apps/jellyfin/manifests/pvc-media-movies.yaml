apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-movies
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Ti
  storageClassName: manual
  volumeName: media-movies
