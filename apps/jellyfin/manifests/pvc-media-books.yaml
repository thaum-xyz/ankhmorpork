apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-books
  namespace: jellyfin
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4000Gi
  storageClassName: manual
  volumeName: media-books
