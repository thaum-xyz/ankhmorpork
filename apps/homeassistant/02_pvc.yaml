---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: config
  namespace: homeassistant
spec:
  storageClassName: "managed-nfs-storage"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
