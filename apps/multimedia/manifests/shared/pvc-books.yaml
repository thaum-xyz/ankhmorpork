apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: books
  namespace: multimedia
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4000Gi
  storageClassName: manual
  volumeName: books
