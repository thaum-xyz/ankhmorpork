apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tv
  namespace: multimedia
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 4000Gi
  storageClassName: manual
  volumeName: tv
