apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads
  namespace: multimedia
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 120Gi
  storageClassName: manual
  volumeName: downloads
