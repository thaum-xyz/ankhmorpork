apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloaded
  namespace: multimedia
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: qnap-nfs-storage
