apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transmission-appdata
  namespace: multimedia
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
  storageClassName: qnap-nfs
