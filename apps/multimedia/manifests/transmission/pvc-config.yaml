apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transmission-config
  namespace: multimedia
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Mi
  storageClassName: longhorn-r2
