apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: exporter
    app.kubernetes.io/name: sonarr-exportarr
    app.kubernetes.io/part-of: sonarr
    app.kubernetes.io/version: 0.3.4
  name: sonarr-exportarr
  namespace: multimedia
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: exporter
      app.kubernetes.io/name: sonarr-exportarr
      app.kubernetes.io/part-of: sonarr
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: exporter
        app.kubernetes.io/name: sonarr-exportarr
        app.kubernetes.io/part-of: sonarr
        app.kubernetes.io/version: 0.3.4
    spec:
      containers:
      - args:
        - sonarr
        env:
        - name: URL
          value: http://sonarr.multimedia.svc:7878
        - name: PORT
          value: "9707"
        envFrom:
        - secretRef:
            name: sonarr-apikey
        image: onedr0p/exportarr:v0.3.4
        imagePullPolicy: IfNotPresent
        name: sonarr-exportarr
        ports:
        - containerPort: 9707
          name: metrics
        readinessProbe:
          failureThreshold: 5
          httpGet:
            path: /healthz
            port: metrics
          periodSeconds: 10
        resources:
          limits:
            cpu: 50m
            memory: 100Mi
          requests:
            cpu: 1m
            memory: 11Mi
      nodeSelector:
        kubernetes.io/arch: amd64
      restartPolicy: Always
      serviceAccountName: sonarr-exportarr
