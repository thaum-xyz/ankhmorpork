# Config reference: https://github.com/minio/operator/tree/master/helm/operator

console:
  ingress:
    enabled: false

operator:
  replicaCount: 2
  resources:
    requests:
      cpu: 200m
      memory: 256Mi
      ephemeral-storage: 500Mi