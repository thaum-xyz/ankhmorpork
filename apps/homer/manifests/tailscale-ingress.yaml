apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: funnel
  namespace: homer
  annotations:
    tailscale.com/funnel: "true"
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: homer
      port:
        number: 8080
  tls:
    - hosts:
        - homer
