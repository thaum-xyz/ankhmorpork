apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: reloader
  namespace: homer
spec:
  filterTags:
    pattern: ^[0-9]+.[0-9]+.[0-9]+
  imageRepositoryRef:
    name: reloader
  policy:
    semver:
      range: '>=0.0.0'
