apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: homer
    app.kubernetes.io/part-of: homer
    app.kubernetes.io/version: 22.11.2
  name: homer
  namespace: homer
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/component: server
      app.kubernetes.io/name: homer
      app.kubernetes.io/part-of: homer
  template:
    metadata:
      annotations:
        checksum.config/md5: 2b21a815b9ee8ad7574b15c6708dfe53
      labels:
        app.kubernetes.io/component: server
        app.kubernetes.io/name: homer
        app.kubernetes.io/part-of: homer
        app.kubernetes.io/version: 22.11.2
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app.kubernetes.io/name
                  operator: In
                  values:
                  - homer
              topologyKey: kubernetes.io/hostname
            weight: 100
      containers:
      - image: b4bz/homer:v22.11.2
        imagePullPolicy: IfNotPresent
        name: homer
        ports:
        - containerPort: 8080
          name: http
        resources:
          limits:
            cpu: 10m
            memory: 100Mi
          requests:
            cpu: 2m
            memory: 3Mi
        volumeMounts:
        - mountPath: /www/assets/config.yml
          name: config
          subPath: config.yml
      restartPolicy: Always
      serviceAccountName: homer
      volumes:
      - configMap:
          name: homer-config
        name: config
