apiVersion: v1
kind: Service
metadata:
  labels:
    app: recipes
  name: recipes
  namespace: tandoor
spec:
  ports:
  - name: gunicorn
    port: 8080
    protocol: TCP
    targetPort: gunicorn
  selector:
    app: recipes
