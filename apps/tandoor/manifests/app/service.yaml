apiVersion: v1
kind: Service
metadata:
  name: recipes
  namespace: tandoor
  labels:
    app: recipes
spec:
  selector:
    app: recipes
  ports:
  - port: 8080
    targetPort: gunicorn
    name: gunicorn
    protocol: TCP
