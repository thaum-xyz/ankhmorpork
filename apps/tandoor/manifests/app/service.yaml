apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: tandoor
    app.kubernetes.io/version: 1.4.9
  name: tandoor
  namespace: tandoor
spec:
  ports:
  - name: gunicorn
    port: 8080
    protocol: TCP
    targetPort: gunicorn
  selector:
    app: recipes
