apiVersion: v1
kind: Service
metadata:
  name: db
spec:
  ports:
  - name: postgresql
    port: 5432
    protocol: TCP
    targetPort: postgresql
  selector:
    app: db
  sessionAffinity: None
  type: ClusterIP
