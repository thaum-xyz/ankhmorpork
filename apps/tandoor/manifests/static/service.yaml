apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: tandoor
    app.kubernetes.io/version: 1.4.9
  name: tandoor-static
  namespace: tandoor
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: static
