apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: tandoor
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: qnap-nfs
