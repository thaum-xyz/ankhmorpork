apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-library
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
  storageClassName: qnap-nfs
#  resources:
#    requests:
#      storage: 4000Gi
#  storageClassName: manual
#  volumeName: immich-library
