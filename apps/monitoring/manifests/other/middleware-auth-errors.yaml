apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  #name: auth-redirect
  name: auth-errors
  namespace: monitoring
spec:
 errors:
   status:
   - "401-403"
   query: "/oauth/sign_in"
   service:
     name: oauth2-proxy
     namespace: auth
     port: 4180
