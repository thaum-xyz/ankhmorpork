apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: webapp
    app.kubernetes.io/name: logseq
  name: logseq
  namespace: logseq
spec:
  clusterIP: None
  ports:
  - name: http
    port: 80
    targetPort: http
  selector:
    app.kubernetes.io/component: webapp
    app.kubernetes.io/name: logseq
