apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: library-old
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 600Gi
  storageClassName: manual
  volumeName: library-old
