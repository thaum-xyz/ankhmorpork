apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: library-old
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4000Gi
  storageClassName: manual
  volumeName: library-old
